<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.springboot.mapper.TestMapper">
    <select id="test" resultType="java.util.HashMap">
		select * from svc_log limit 0,10
	</select>

	<select id="getSvclog" parameterType="string" resultType="java.util.HashMap">
		select * from svc_log where rid = #{rid}
	</select>
</mapper> 